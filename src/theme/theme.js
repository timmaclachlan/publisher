import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#14602c',
      dark: '#323332',
      light: '#329644'
    },
    secondary: {
      main: '#ea4e82',
      dark: '#d81b4d',
      light: '#f5f1e8'
    },
    whitepanel: {
      main: '#ffffff'
    }
  }
});